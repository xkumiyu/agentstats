# @xkumiyu/agentstats

Install the agentstats CLI with npm:

```sh
npm install --global @xkumiyu/agentstats
```

The package includes the native executable for each supported platform and
selects the matching one when the command runs.

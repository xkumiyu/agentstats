# @xkumiyu/agentstats

Install the agentstats CLI with npm:

```sh
npm install --global @xkumiyu/agentstats
```

The package downloads the matching native executable from the corresponding
GitHub Release during installation and verifies its SHA-256 checksum.
